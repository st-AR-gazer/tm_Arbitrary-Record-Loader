const vec4 AccentCol       = vec4(0.42f, 0.78f, 0.44f, 1.0f);
const vec4 AccentDimCol    = vec4(0.30f, 0.58f, 0.32f, 1.0f);
const vec4 AccentBrightCol = vec4(0.55f, 0.90f, 0.55f, 1.0f);
const vec4 SidebarBg       = vec4(0.10f, 0.10f, 0.12f, 1.0f);
const vec4 SidebarSelBg    = vec4(0.20f, 0.35f, 0.22f, 0.70f);
const vec4 SidebarHoverBg  = vec4(0.18f, 0.28f, 0.20f, 0.50f);
const vec4 ContextBg       = vec4(0.12f, 0.12f, 0.14f, 1.0f);
const vec4 HeaderBg        = vec4(0.16f, 0.28f, 0.18f, 0.80f);
const vec4 HeaderHoverBg   = vec4(0.22f, 0.38f, 0.24f, 0.80f);
const vec4 HeaderActiveBg  = vec4(0.28f, 0.48f, 0.30f, 0.80f);
const vec4 UnloadBtnCol    = vec4(0.70f, 0.25f, 0.25f, 1.0f);
const vec4 UnloadBtnHover  = vec4(0.85f, 0.30f, 0.30f, 1.0f);
const vec4 UnloadBtnActive = vec4(0.95f, 0.35f, 0.35f, 1.0f);

const float IconButtonWidth = 34.0f;
const float LoadedActionsColWidth = 190.0f;
const float AllGhostActionsColWidth = 84.0f;

